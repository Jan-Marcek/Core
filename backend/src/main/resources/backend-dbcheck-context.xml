<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd">

    <bean id="configuration"  class="cz.cuni.mff.xrg.odcs.commons.app.conf.AppConfig">
        <property name="ignoreUnresolvablePlaceholders" value="true"/>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="locations">
            <list>
                <value>file:${configFileLocation}</value>
            </list>
        </property>
    </bean>

    <!--
    <import resource="classpath:commons-app-context.xml"/>
     -->
    
    <!-- bean for initializing DB or running updating scripts -->
    <bean id="dbUpdater" class="cz.cuni.mff.xrg.odcs.db.updater.DBUpdater">
    	<constructor-arg name="dbType" value="${database.sql.platform}" />
    	<constructor-arg name="connectionString" value="${database.sql.url}" />
    	<constructor-arg name="driver" value="${database.sql.driver}" />
    	<constructor-arg name="username" value="${database.sql.user}" />
    	<constructor-arg name="password" value="${database.sql.password}" />
    </bean>

</beans>